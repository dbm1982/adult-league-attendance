name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit wake endpoint
        run: curl -s "https://adult-league-attendance-kb768k8aqwvdpknbz2wk8k.streamlit.app/?wake=1"
